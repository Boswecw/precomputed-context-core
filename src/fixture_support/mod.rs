//! Canonical shared fixture support for governed proof and test scenarios.
//
// This module is intentionally kept behind the `test-support` feature
// so shared fixtures do not become part of the normal crate surface.

// Add submodules here as you create them.
// pub mod builders;
// pub mod ids;
// pub mod mutations;
// pub mod scenarios;
