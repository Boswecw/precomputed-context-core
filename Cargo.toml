[package]
name = "precomputed-context-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "precomputed_context_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
